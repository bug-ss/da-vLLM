# The exact serving stack the reported results were produced with (guide 2).
# Install order matters for the two that need special handling; see the notes.
#
#   python -m venv .venv && . .venv/bin/activate      # Python 3.13
#   pip install -r requirements-serve.txt
#   pip install --no-build-isolation causal-conv1d==1.6.1
#   pip install "deep-gemm @ git+https://github.com/deepseek-ai/DeepGEMM@v2.1.1.post3"
#   pip install -e .
#
# GPU: NVIDIA B200 for every evaluation and efficiency number, one engine per
# GPU, tensor parallel 1. bf16 weights and bf16 KV cache throughout.

torch==2.11.0            # CUDA 13 wheels: cudnn 9.19, nccl 2.28.9, cublas 13.1
vllm==0.20.2
transformers==5.5.4
triton==3.6.0
flashinfer-python==0.6.8.post1   # pulled by vLLM; not used for attention
flash-linear-attention==0.5.0    # Qwen 3.5/3.6 Gated DeltaNet layers
datasets==3.6.0

# --- needs --no-build-isolation, so it is not listed above -------------------
# causal-conv1d==1.6.1
#
# --- git tag, so it is not listed above --------------------------------------
# deep-gemm @ git+https://github.com/deepseek-ai/DeepGEMM@v2.1.1.post3
#
# --- deliberately absent ------------------------------------------------------
# flash-attn: there is NO separate wheel. The FlashAttention path is vLLM's
# bundled FLASH_ATTN backend; the Triton path is vLLM's TRITON_ATTN backend.
#
# --- pin your model revisions -------------------------------------------------
# The original work pinned no HuggingFace revision, which makes its numbers
# unreproducible in principle. Pin them:
#   AutoTokenizer.from_pretrained(model, revision="<sha>")
